#include<iostream>
#include<vector>
using namespace std;
int main(){
    const int MAX_N = 18;
    int N;
    cin >> N;
    vector<int> ｘ(N);
    vector<int> ｃ(N);
    vector<int> ｖ(N);
    for(int i=0; i<N; i++) cin >> x[i];
    for(int i=0; i<N; i++) cin >> c[i];
    for(int i=0; i<N; i++) cin >> v[i];
    return 0;
}